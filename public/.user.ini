; Límites para subir vídeos largos desde el panel (editor de contenido).
; Hostinger (PHP-FPM/LiteSpeed) aplica este .user.ini en el directorio public.
upload_max_filesize = 1024M
post_max_size = 1024M
max_execution_time = 1200
max_input_time = 1200
memory_limit = 512M
